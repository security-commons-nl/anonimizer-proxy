{
  "name": "anonimizer-proxy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Cloudflare Worker — minimal forward-only proxy to Mistral API for anonimizer-browser.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260907.1",
    "typescript": "~7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.1"
  }
}
